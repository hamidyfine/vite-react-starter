import type { MantineThemeOverride } from '@mantine/core';

export const theme: MantineThemeOverride = {
    fontFamily: 'Quicksand Variable, sans-serif',
    headings: { fontFamily: 'Quicksand Variable, sans-serif' },
};
